# Project Memory

- Canonical project memory path is `.camie/project-memory.md`; `.cami` is incorrect for this repo.
- KamiCode snapshots project memory through `apps/server/src/provider/ProjectMemory.ts` at provider session creation and injects that snapshot only on the first user turn of the session.
- Codex receives the first-turn project memory snapshot as developer instructions; Claude, OpenCode, and Cursor receive it as a prompt prefix because those adapters do not share the Codex collaboration-mode developer-instruction channel.
- Project memory updates should stay concise, durable, deduplicated, and must not include secrets, tokens, passwords, private keys, or transient debug logs.
- Test harness run history is exposed through `GET /api/test-harness/runs?cwd=<project cwd>` and rendered in the chat header by `apps/web/src/components/TestHarnessRunsControl.tsx`.
- Inline evidence display parses both Codex-style dynamic-tool `data.contentItems` payloads and Claude MCP-style `data.result.content` text payloads in `apps/web/src/session-logic.ts`; keep both shapes covered when changing harness result rendering.
- KamiCode test harness supports `auth: { type: "kamicode-pairing" }`; Codex runtime mints a short-lived pairing credential through `ServerAuth`, the browser uses the real `/pair#token=...` flow, then storage state is saved per project for reuse.
- `apps/web/src/components/TestHarnessRunsControl.tsx` has both the compact header popover and a larger Test Runs sheet; both refresh while open and show inline screenshots, trace, summary, and issue counts.
- Test Mode live UI verification on 2026-05-27 confirmed that Codex sent `interactionMode: "test"` and called `kamicode.kami_test_harness` with `auth: { type: "kamicode-pairing" }`; the successful run reached the authenticated KamiCode shell at `/` with visible text like `No active thread` and no harness-reported console or network failures.
- GitHub user-auth bootstrap is optional UI state; if `/api/user/session` fails or redirects during app boot, KamiCode should fail closed to user auth disabled and keep the main shell usable.
- Codex Test Mode now auto-applies KamiCode pairing auth inside the harness bridge for local KamiCode dev targets like `http://127.0.0.1:5733` and `/pair` when the runtime can issue a short-lived credential.
- Test Mode browser verification should be headless and recorded by default. Use visible browser mode only after an explicit user request; harness artifacts should include screenshots, trace, and video when possible.
- Auth-gated Test Mode verification must set `authExpectation: "authenticated"` and cannot treat a login/auth page as success. If no saved session, local credentials, seeded user, bypass, or mock auth exists, ask with `request_user_input` question id `test_auth_strategy` using options: `I'll provide sign-in credentials`, `Create permanent user`, and `Create temporary user`.
- Internal Windows releases are produced by tagging `vX.Y.Z`; `.github/workflows/release.yml` builds the x64 NSIS installer and publishes GitHub Release assets.
- After the upstream T3 Code sync, the workspace toolchain is `pnpm@10.24.0` + vite-plus (`vp`) + `@effect/tsgo` on Node 24; `bun.lock` was removed and `pnpm-lock.yaml` is the only lockfile.
- Background turn-completion audio is app-wide in `apps/web/src/components/TurnCompletionSoundCoordinator.tsx`; it should trigger only after the latest turn is completed and the thread session is no longer working, and only when the document is visible but not focused or hidden.
- Background turn-completion audio uses a seen-completion key tracker seeded from current store state, plus a short mount-time freshness guard, so it can alert for newly observed settled snapshots without replaying old completed turns. Audio priming listens in capture phase for pointer/key events to avoid composer event propagation blocking Web Audio unlock.

## Phase 6: Shared Projects + Runtime Continuation

- Phase 6 should be simplified from "Remote/VPS Environments" to "Shared Projects + Runtime Continuation": the durable goal is remote team collaboration, not making VPS the center of the product.
- Do not assume Codex must run on a VPS. A shared project should preserve code context, project memory, shared threads, environments, and artifacts so authorized teammates can continue work from their own runtime: local desktop, SSH/VPS remote, or a future hosted worker.
- Keep these concepts separate:
  - Shared Project: durable collaboration object containing repo/branch metadata, project memory/context bundle, AGENTS/instructions snapshot, environments, members/roles, shared threads, and artifacts.
  - Shared Thread: a specific unit of work/conversation inside a project, with messages, agent decisions, runtime metadata, test artifacts, and handoff state.
  - Runtime: the machine/session doing execution now, such as local desktop, SSH/VPS, or future cloud worker.
  - Environment: a target app URL/config for testing, such as local dev, preview deploy, VPS staging, or production.
- Sharing a project must not automatically publish every local thread. Existing local threads stay private until explicitly shared; new threads in a shared project may default to shared with a visible private/shared control.
- Do not automatically move a local project to VPS when it is shared. Sharing creates/syncs durable project state; attaching a remote runtime is a separate explicit action.
- Provider credentials and credit limits are runtime/user-local concerns. If a remote runtime/provider is unavailable or out of credits, the user should be able to continue locally and choose whether that continuation syncs back to the shared thread or remains private.
- Project invites should be one-time, expiring, and bound to a specific GitHub identity. Invite acceptance must verify the signed-in KamiCode account's GitHub identity before granting project access, with clear rejection for mismatched accounts.
- Project sharing, invite codes, team members, roles, environments, branch/deploy URL associations, and runtime attachments should be managed from the project settings page.
- A teammate initializing a shared project should receive a bootstrap manifest: repo URL, default branch, current branch/SHA, context bundle, known environments, shared threads, and available runtimes. The teammate can open an existing clone, clone from GitHub, or attach a remote runtime.
- Code handoff should track repo remote, branch, HEAD SHA, dirty status, and optionally a patch artifact for uncommitted local changes. Dirty work must be visible before sharing or handoff.
- Phase 6 MVP tasks should be testable backend/frontend pairs: collaboration control plane, GitHub identity binding, publish local project, context bundle sync, members/roles, GitHub-bound invites, initialize shared project, thread visibility, shared thread sync, code state handoff, runtime registry, provider/profile boundary, multiple project environments, and branch/deploy URL association.
- VPS/deploy sync, mobile web continuation, shared test artifacts, and automatic remote tests after push/deploy remain important but can follow once manual shared-project continuation works cleanly.
- Phase 6 tasks 1-15 now have a first-pass implementation: contracts live in `packages/contracts/src/sharedProject.ts`; SQLite persistence is migration `036_SharedProjects` (renumbered from `034` in the upstream sync); the server service/routes live under `apps/server/src/sharedProjects`; and the settings command center is `/settings/shared-projects`.
- Shared-project context sync intentionally stores project memory, selected instructions, package-manager/scripts, repo metadata, and environment variable names only; it excludes secret values, raw `.env` contents, auth state, private keys, and provider credentials.
- Shared-project invite claiming currently compares the authenticated user's normalized GitHub login and, when an invite has `githubId`, the GitHub id; invite creation from the settings UI sends only `githubLogin`, and user auth does not yet fetch/store verified GitHub emails.
- Hosted collaboration backend scaffold lives in `apps/collab-server`; it is a standalone Node service backed by Postgres, exposes central `/api/shared-projects/*` routes, uses `KAMICODE_COLLAB_SERVER_TOKEN` plus GitHub identity headers for MVP auth, and includes Docker/systemd deployment docs.
- Shared-project SSH credentials belong to the main backend `apps/server`, not `apps/collab-server`; migration `037_SharedProjectSshCredentials` (renumbered from `035` in the upstream sync; shared-session snapshots are `038_SharedSessionSnapshots`) stores encrypted credential envelopes in SQLite, with the AES-GCM key held by `ServerSecretStore`.
- Package scripts that execute TypeScript files with Node should pass `--experimental-strip-types`; Node 22.15 otherwise fails with `ERR_UNKNOWN_FILE_EXTENSION` for `.ts` entrypoints like `scripts/dev-runner.ts`, `apps/server/src/bin.ts`, and `apps/collab-server/src/index.ts`.
- TypeScript scripts that need main-module detection should use `scripts/lib/node-main.ts` or a `pathToFileURL(process.argv[1])` check; Node 22.15 does not make `import.meta.main` true, which causes scripts to load and exit silently.
- Desktop SSH environment connection now supports a one-time password field in the local Connections settings flow; the password is passed through desktop IPC only for the current SSH attempt and is not saved in the saved environment registry.
- SSH remote runtime bootstrap installs a user-local Node fallback under `~/.t3/node` on Linux x64/arm64 hosts when non-interactive SSH cannot find a suitable Node from PATH, Volta, asdf, mise, fnm, nodenv, or nvm; installer failures now surface concrete download/checksum/extract/runtime errors instead of being swallowed as a generic missing-Node error.
- SSH remote T3 CLI startup should provision in the foreground before `serve`: it prefers the cached runner under `~/.t3/runner/t3-cli` and direct `node_modules/.bin/t3` execution, treats `npx`/`npm exec` as fallback paths, and first remote server readiness allows a cold install window instead of killing npm after a short timeout.
- Desktop SSH dev runs now resolve the remote T3 package from the selected update channel instead of forcing `t3@nightly`; this avoids broken nightly packages unless the user explicitly selects the nightly channel.
- The SSH Add Environment suggested-host button should merge matching typed host/username/port fields before connecting, so clicking a known-hosts row does not silently fall back to the local OS username.
- Desktop SSH remote auth endpoints return JSON ISO strings for `expiresAt`; decode them with auth `*Json` schemas and encode IPC auth results as JSON-shaped values before they reach the renderer.
- The hosted collaboration backend is deployed on `admin@144.91.68.19` as systemd service `kamicode-collab`, installed under `/opt/kamicode-collab`, backed by local Postgres database `kamicode_collab`, and exposed at `http://144.91.68.19:8787`; secrets live only in `/opt/kamicode-collab/.env`.
- Hosted shared-project routing is enabled through the main desktop backend with `T3CODE_COLLAB_SERVER_URL` and `T3CODE_COLLAB_SERVER_TOKEN`; the browser still calls the local/primary backend, which proxies shared-project control-plane requests to the hosted collab server using server-side identity headers so the hosted token is never exposed to the renderer.
- Hosted shared-session import is local-copy only: the desktop backend fetches the visible snapshot from the hosted collab server, then creates a new local orchestration thread in the importing user's selected project.
- The hosted collab server authorizes project membership by internal user id, GitHub id, or normalized GitHub login so the same GitHub account can access its shared projects from another desktop even if that desktop generated a different local Kami user id.
- GitHub user auth uses `read:user` identity, stores `github_id`/login/avatar in SQLite, and issues a signed `t3_user_session` cookie plus a DB-backed session. Web/confidential OAuth requires `T3CODE_GITHUB_OAUTH_CLIENT_ID` and `T3CODE_GITHUB_OAUTH_CLIENT_SECRET`; desktop device flow requires only `T3CODE_GITHUB_OAUTH_CLIENT_ID` so packaged desktop builds do not embed a GitHub secret.
- `turbo.json` and its `globalEnv` mechanism are gone after the upstream sync; env vars (including the GitHub OAuth names) are read directly from the process environment, so `dev:desktop` no longer needs a globalEnv allowlist to reach the Electron/desktop backend process.
- Shared-project UX model: enabling a project for sharing creates the shared project container, but users share individual KamiCode sessions/threads from the sidebar. The sidebar thread row has a hover "Share session" action that ensures the project is sharing-enabled, waits for thread detail, then publishes that specific session's messages/code state.
- Target session sharing model: clicking Share creates a point-in-time session snapshot linked to the shared project/session; imported or continued work never updates an existing share. To hand off newer work, the user must explicitly create another snapshot share from their local session.
- Shared sessions now persist a `sessionSnapshot` with message turn IDs, completed timestamps, attachment metadata, activity/tool payloads, proposed plans, checkpoints, latest/queued turn state, session state, and repo pointers; snapshots intentionally exclude codebase files, project folder archives, raw `.env`, secret values, private keys, auth state, and provider credentials.
- Shared-session import is wired through `/api/shared-projects/threads/import`: authorized users can import a visible shared session snapshot into a selected local project, which creates a new local orchestration thread with remapped message/activity ids while preserving turn links, activities, proposed plans, checkpoints, and valid chat attachments for local continuation.
- Shared-project owners can stop sharing through `/api/shared-projects/delete`; it deletes the shared project container and cascades shared members, invites, threads, runtimes, environments, deploys, and SSH credentials while leaving local projects/sessions/files untouched.
- Hosted shared-project routing can be configured from Shared Projects settings through `hostedCollaboration`; env vars `T3CODE_COLLAB_SERVER_URL`/`T3CODE_COLLAB_SERVER_TOKEN` still override UI settings. The token is stored through `ServerSecretStore` and redacted from client settings snapshots. Desktop SSH auto-deploy clones `https://github.com/camie-ace/KamiCode` on the selected saved SSH connection, uses Docker for Postgres, and starts `@t3tools/collab-server`.
- Shared Projects settings normalizes desktop collab SSH deploy failures for missing remote prerequisites like Docker, Git, curl, unusable Docker permissions, non-Linux targets, and SSH authentication issues instead of showing raw Electron/SSH exception text. When Docker is missing, the UI exposes an explicit "Install Docker & deploy" recovery action; the desktop deployer only runs Docker installation when that IPC option is set, and it requires root or passwordless sudo.
- The desktop collab deployer has a bounded allowlisted repair path behind the explicit repair/deploy UI action. It can install missing `git`/`curl` via supported package managers, install Docker through the Docker convenience script, and try to start Docker; it does not run arbitrary AI-generated SSH commands.
- Desktop collab deploy scripts must not use `String.raw` around shell scripts containing escaped JS template placeholders like `\${1:-}`; raw templates preserve the backslash and prevent remote bash from expanding positional parameters. Use a normal template literal so shell variables expand at runtime.
- Shared Projects settings captures collaboration SSH deploy failures and can hand them to a normal KamiCode agent thread as a sanitized AI repair prompt; this intentionally preserves the regular approval flow for any remote commands instead of adding arbitrary AI-generated commands to the deployer.
- Shared Projects deploy failure UX now prioritizes in-place `Auto repair & retry`, which reruns the bounded desktop SSH repair/deploy flow using the one-time password still held in the settings form. The AI repair thread remains a fallback and cannot access the form password or invoke renderer-only desktopBridge calls.
- Desktop collab deployer shell scripts define `need_command`; do not call helper functions that are absent from the generated bash script because failures only surface on the remote host.
- Desktop collab deploy still runs `bun install --frozen-lockfile --ignore-scripts` on the remote clone because the root monorepo `prepare` script (`effect-language-service patch`) is dev-only and fails in the minimal VPS deployment environment; with `bun.lock` removed from the workspace this remote deploy script is being ported off bun to pnpm.
- Desktop collab deploy can fall back to a locally built `apps/collab-server/dist/index.mjs` bundle when the cloned GitHub repo does not yet contain the unpushed `apps/collab-server` workspace. Build `@t3tools/collab-server` locally before retrying dev deploys that depend on unpublished collab-server code.
- Desktop collab deploy is intended to be idempotent per SSH user: it manages `~/.kamicode-collab`, updates or reclones the repo/bundle there, reuses an existing `KAMICODE_COLLAB_SERVER_TOKEN` from `~/.kamicode-collab/.env` when present, starts/reuses the Docker Postgres container, and restarts the user service or fallback process.
- Shared Projects settings must await `server.updateSettings` when saving/clearing/deploying `hostedCollaboration` before reloading hosted shared projects; otherwise the reload can race ahead of secret persistence and show a false authentication error.
- Nightly desktop releases are tag-driven through `.github/workflows/release.yml`; tags matching `v<semver>-nightly.<YYYYMMDD>.<run>` build a Windows x64 prerelease with `make_latest: false`, so they do not become stable/latest.
- The release workflow passes GitHub secret `T3CODE_GITHUB_OAUTH_CLIENT_ID` into `scripts/build-desktop-artifact.ts` so packaged desktop builds include the public desktop GitHub OAuth client id; desktop releases must not require or embed the GitHub OAuth client secret.
- Nightly `v0.0.25-nightly.20260612.2` was built successfully from `294f6329` and includes the GitHub OAuth client-id release workflow wiring; manual run `27416512642` was later started for `0.0.25-nightly.20260612.3` on the same branch.
- Workflow Mode docs now live in `docs/workflows/Workflow.md` and `docs/workflows/sub-agents.md` and describe a planning-first model, but the current web implementation is still mostly single-thread/activity-log driven: `PlanSidebar.tsx` renders planned lanes/cards from workflow activities and `ChatView.tsx` synthesizes many handoff/evidence/result records after the lead turn completes rather than from real sub-agent session events.
- Workflow runtime records now have explicit shared payload schemas in `packages/contracts/src/orchestration.ts` for `workflow.lane.guidance`, `workflow.lane.stopped`, `workflow.lane.control`, `workflow.handoff`, `workflow.evidence`, `workflow.route-back`, `workflow.lead.synthesis`, `workflow.memory.update`, and status/result records; lane-targeted runtime records should key by stable `laneId` with `laneRole` only as a human-readable label.
- `thread.workflow.record` is now discriminated by `kind` in `packages/contracts/src/orchestration.ts`, so workflow payload validation is kind-specific. Minimum dynamic lane lifecycle kinds now include `workflow.lane.started`, `workflow.lane.completed`, and `workflow.lane.blocked`, and `workflow.verifier.result` requires a stable `laneId`.
- Workflow panel hierarchy in `apps/web/src/components/PlanSidebar.tsx` now centers on four layers: overview, collapsed agent cards, one selected Lead/sub-agent detail panel, and a unified clickable `Lead inbox`; duplicate handoff/evidence/memory sections were removed in favor of the inbox cards.
- Workflow planning is moving from a static lane roster toward dynamic sub-agents: `apps/server/src/orchestration/decider.ts` now infers roles like `UI Builder` and `Backend Builder` from the task shape, and `apps/web/src/components/PlanSidebar.tsx` now renders planned sub-agent cards from `workflow.planned`/`workflow.customized` payloads instead of always manufacturing the old fixed `Planner/Builder/Verifier/Documenter` set.
- Workflow child-agent runtime is now partially implemented in `apps/web/src/components/ChatView.tsx`: Start Workflow records `workflow.started`, launches dependency-free planned lanes as child threads/turns, applies lane model/reasoning/fast-mode settings to child `modelSelection`, guidance re-triggers a lane through the child thread path, child-thread settlement appends parent `workflow.handoff`/`workflow.lane.completed`, and dependent lanes launch after prerequisite lane ids complete.
- Workflow verification status from 2026-06-26: `pnpm install --frozen-lockfile` restored missing Clerk packages; `pnpm exec vp test packages/contracts/src/orchestration.test.ts apps/server/src/orchestration/decider.projectScripts.test.ts` passed 57 tests; `pnpm exec vp check` passed with warnings only; and full `pnpm exec vp run typecheck` passed across all 16 packages. A later broad `pnpm exec vp test` run was started but aborted before completion.
- Media handling V1 now includes shared `MediaArtifact`/image/gif/video/file attachment contracts, server attachment normalization/storage, provider safeguards for non-image media, richer composer attachment chips, inline media artifact cards, a thread-level Media Shelf, workspace video preview access, generated-media path helpers, local media search/ranking helpers, local-search result cards/grid, safe desktop `revealLocalMediaFile` IPC, card Open/Reveal/Copy image/Use actions, and lightweight follow-up context resolution for phrases like "that image", "selected video", and "second one". Manual workflow/media test plan lives at `docs/testing/workflow-media-test-plan.md`. Verification on 2026-06-27 passed `pnpm exec vp check`, full `pnpm exec vp run typecheck`, and focused media/reveal/follow-up/local-search tests; broad Cursor/Grok ACP tests can still fail on Windows with `spawn EFTYPE`, and one Codex text-generation batch can fail if the temp Codex CLI executable is missing.
- Desktop dev custom protocol proxy must strip `Host` and hop-by-hop request headers before forwarding to Vite/backend; forwarding `Host: app` makes Vite reject module requests and leaves the renderer stuck on the boot logo.
- Dev desktop releases are tag-driven through `v<semver>-dev.<YYYYMMDD>.<run>` and publish prerelease assets on the `dev` update channel; Windows artifact names should use `KamiCode-${version}-${arch}.${ext}`, not the upstream `T3-Code` prefix.
- Thread media UX now has a first-class right-panel `Media` surface (`apps/web/src/components/chat/MediaPanel.tsx`) backed by `rightPanelStore` kind `media`; the composer-area `MediaShelf` is collapsed by default and should remain a lightweight launcher/peek rather than the primary gallery.
